<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
              xmlns:viewModels="clr-namespace:MaterialTracking.ViewModels"
             x:Class="MaterialTracking.Views.JobTicketViews"       NavigationPage.HasNavigationBar="False">
    <ContentPage.BindingContext>
        <viewModels:JobTicketViewModels  />
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout>
            <Frame BackgroundColor="{StaticResource Key=YellowColor}" Padding="24" CornerRadius="0">
                <Label Text="JOB TICKET" HorizontalTextAlignment="Center" TextColor="{StaticResource Key=RedColor}" FontSize="36"/>
            </Frame>
            <ScrollView>
                <StackLayout>
                    <Button Text="Machine No" HeightRequest="80" TextColor="White" FontSize="23" Margin="0, 0.5, 0 ,5" Command="{Binding MachineNoCommand }" BackgroundColor="{StaticResource Key=PrimaryColor}"/>
                </StackLayout>
            </ScrollView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>